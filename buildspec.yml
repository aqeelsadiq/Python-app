version: 0.2

phases:
  pre_build:
    commands:
      - echo log in to aws ecr 
      - aws --version
      - aws ecr get-login-password --region us-west-1 | docker login --username AWS --password-stdin 944347495097.dkr.ecr.us-west-1.amazonaws.com
      - REPOSITORY_URI=944347495097.dkr.ecr.us-west-1.amazonaws.com/my-pythton-ecr
      - IMAGE_TAG=latest

  build:
    commands:
      - echo Building started on 'date'
      - echo Building the docekr image
      - docker build -t $REPOSITORY_URI:$IMAGE_TAG .
      - docker tag $REPOSITORY_URI:$IMAGE_TAG $REPOSITORY_URI:$IMAGE_TAG

  post_build:
    commands:docker push $REPOSITORY_URI:$IMAGE_TAGdocker push $REPOSITORY_URI:$IMAGE_TAGdocker push $REPOSITORY_URI:$IMAGE_TAG
      - echo Build completed on `date`
      - echo Pushing the Docker images.
      - docker push $REPOSITORY_URI:$IMAGE_TAG


artifacts:
   path: '**/*'